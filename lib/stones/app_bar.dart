import 'package:flutter/material.dart';

class SteppingStoneAppBar extends StatelessWidget {
  const SteppingStoneAppBar(this.name, {super.key});

  final String name;

  @override
  Widget build(BuildContext context) {
    return AppBar(
    );
  }
}
